// import DropdownMenu from "./DropdownMenu";

// import { DropdownMenuStyles } from "./styles/FormStyles";

// !Create a dropdown component
// !  - props:
// !      - array of options
// !      - data-name (optional)
// !      - callback function to update useForm()
// !  - make like a standalone component:
// !      - width will be 100%
// !      - will always place component inside of a div so that div will determine size and position

// !Create config file in lib to hold options for each dropdown component
// !Create user object in config file
// export default function FilterMenu() {
//   return (
//     <DropdownMenuStyles>
//       <DropdownMenu options={filterOptions} />
//     </DropdownMenuStyles>
//   );
// }
